# Raw source material

This directory preserves the supplied standalone HTML/CSS/JS experiments and
the original Bricks export. Raw source is reference/intake material, not code
that the Phase 0 applications execute. Use the `imports/` and `fixtures/`
contracts before promoting any source into a later conversion pipeline.
