defmodule LiveFrames do
  @moduledoc """
  Reusable, source-independent contracts for the LiveFrames compiler.

  Phase 0 intentionally contains no Bricks parser, ACSS adapter, Design IR,
  component catalogue, or generated UI. Those boundaries are introduced in
  later phases of `docs/00_LIVEFRAMES_MASTER_SPEC.md`.
  """
end
