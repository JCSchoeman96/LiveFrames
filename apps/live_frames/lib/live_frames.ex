defmodule LiveFrames do
  @moduledoc """
  Reusable, source-independent contracts for the LiveFrames compiler.

  Phase 2 owns the source-independent Design IR contract. It intentionally
  contains no Bricks parser, Automatic.css adapter, component catalogue, or
  generated UI. Those boundaries are introduced in later phases of
  `docs/00_LIVEFRAMES_MASTER_SPEC.md`.
  """
end
