# ACSS reference material

`acss.json` is the supplied Automatic.css settings export. The `4.0.1/`
directory contains local research notes and schema references. These files are
inputs for a future clean-room adapter and are not loaded by LiveFrames at
runtime. Check `fixtures/automatic_css/SOURCE.md` for the staged export’s
provenance status.
