# CSS and Tailwind strategy

**Status:** P6.4A architecture proposed — see
`docs/20_P6_4A_NATIVE_STYLING_BRIDGE_ARCHITECTURE.md` for the active styling
bridge contract (library-owned CSS, official Tailwind v4 authority, token bridge,
`@theme` / `@apply` policy, and preview verification role).

This document remains the long-term home for cross-cutting CSS/Tailwind strategy.
Until P6.5 documentation, `docs/20` is the execution authority for native
component styling.
